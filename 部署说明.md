# JUPAS 系统部署说明

## ✅ 部署完成！

项目已成功部署到GitHub和阿里云服务器。

---

## 📦 GitHub仓库

**仓库地址：** https://github.com/keranxu726-maker/jupas-frontend

### 本地开发
```bash
# 克隆项目
git clone git@github.com:keranxu726-maker/jupas-frontend.git

# 安装依赖
cd jupas-frontend
npm install

# 启动开发服务器
npm run dev
```

---

## 🚀 阿里云部署信息

### 服务器信息
- **服务器IP：** 112.124.68.82
- **用户名：** admin
- **项目路径：** /home/admin/jupas-frontend
- **访问地址：** http://112.124.68.82:3002/

### PM2进程信息
- **进程名称：** jupas-frontend
- **运行端口：** 3002
- **进程状态：** online
- **启动方式：** pm2 serve (SPA模式)

### 常用命令

#### SSH连接服务器
```bash
ssh admin@112.124.68.82
```

#### 查看应用状态
```bash
pm2 list
pm2 status jupas-frontend
pm2 logs jupas-frontend
```

#### 重启应用
```bash
pm2 restart jupas-frontend
```

#### 停止应用
```bash
pm2 stop jupas-frontend
```

#### 删除应用
```bash
pm2 delete jupas-frontend
```

---

## 🔄 更新部署流程

当代码有更新时，按以下步骤重新部署：

### 1. 本地提交代码到GitHub
```bash
cd /Users/shanshan/Documents/sss/jupas-frontend
git add .
git commit -m "更新说明"
git push origin main
```

### 2. 服务器拉取最新代码
```bash
ssh admin@112.124.68.82 "cd ~/jupas-frontend && git pull"
```

### 3. 重新构建
```bash
ssh admin@112.124.68.82 "cd ~/jupas-frontend && source ~/.nvm/nvm.sh && npm install && npm run build"
```

### 4. 重启PM2服务
```bash
ssh admin@112.124.68.82 "source ~/.nvm/nvm.sh && pm2 restart jupas-frontend"
```

### 一键部署脚本
```bash
# 在本地执行
cd /Users/shanshan/Documents/sss/jupas-frontend
git add . && git commit -m "更新" && git push origin main
ssh admin@112.124.68.82 "cd ~/jupas-frontend && git pull && source ~/.nvm/nvm.sh && npm install && npm run build && pm2 restart jupas-frontend"
```

---

## 🌐 访问方式

### 开发环境（本地）
- 地址：http://localhost:3001/
- 说明：本地开发调试使用

### 生产环境（阿里云）
- 地址：http://112.124.68.82:3002/
- 说明：线上正式环境

---

## 📋 测试账号

### 管理员账号
- 用户名：`admin`
- 密码：`admin123`

### 学生账号
- 用户名：`student1`
- 密码：`123456`
- 权益点数：10

---

## 🛠 技术栈

- **前端框架：** React 18
- **路由：** React Router 6
- **构建工具：** Vite
- **进程管理：** PM2
- **版本控制：** Git + GitHub
- **部署方式：** SSH + PM2

---

## 📝 项目结构

```
jupas-frontend/
├── dist/                 # 构建输出目录（生产环境）
├── src/                  # 源代码
│   ├── components/       # 公共组件
│   ├── pages/           # 页面组件
│   ├── utils/           # 工具函数和Mock数据
│   ├── constants/       # 常量定义
│   └── styles/          # 全局样式
├── package.json         # 项目配置
├── vite.config.js       # Vite配置
├── README.md            # 项目说明
├── 启动说明.md          # 本地启动说明
└── 部署说明.md          # 本文件

```

---

## ⚠️ 注意事项

1. **端口占用：** 确保服务器3002端口未被其他服务占用
2. **防火墙：** 如无法访问，检查阿里云安全组是否开放3002端口
3. **Node版本：** 服务器需要Node.js 16+版本
4. **PM2自启动：** 已配置pm2 save，服务器重启后应用会自动启动
5. **数据持久化：** 当前使用LocalStorage，生产环境建议接入真实后端API

---

## 🔐 安全建议

1. 修改默认测试账号密码
2. 配置HTTPS证书（使用Let's Encrypt）
3. 设置Nginx反向代理（推荐）
4. 定期备份代码和数据
5. 配置日志监控和告警

---

## 📞 问题排查

### 应用无法访问
```bash
# 检查PM2状态
pm2 status

# 查看应用日志
pm2 logs jupas-frontend

# 检查端口占用
netstat -tunlp | grep 3002
```

### 更新后页面未变化
```bash
# 清除浏览器缓存
# 或在浏览器中强制刷新（Ctrl+Shift+R / Cmd+Shift+R）

# 确认dist目录已更新
ssh admin@112.124.68.82 "ls -lh ~/jupas-frontend/dist"
```

---

## 🎉 部署完成时间

- **GitHub推送：** 2025-12-14
- **阿里云部署：** 2025-12-14
- **PM2进程ID：** 1

---

**祝使用愉快！** 🚀

