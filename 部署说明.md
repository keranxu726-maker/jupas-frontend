# JUPAS 系统部署说明

## ✅ 部署完成！

项目已成功部署到GitHub和阿里云服务器。

---

## 📦 GitHub仓库

**仓库地址：** https://github.com/keranxu726-maker/jupas-frontend

### 本地开发
```bash
# 克隆项目
git clone git@github.com:keranxu726-maker/jupas-frontend.git

# 安装依赖
cd jupas-frontend
npm install

# 启动开发服务器
npm run dev
```

---

## 🚀 阿里云部署信息

### 服务器信息
- **服务器IP：** 112.124.68.82
- **用户名：** admin
- **项目路径：** /home/admin/jupas-frontend
- **访问地址：** http://112.124.68.82/jupas/

### 部署方式
- **Web服务器：** Nginx
- **访问路径：** /jupas/
- **静态文件目录：** /home/admin/jupas-frontend/dist
- **部署方式：** Nginx直接serve静态文件（与xunji复用80端口）

### 常用命令

#### SSH连接服务器
```bash
ssh admin@112.124.68.82
```

#### 查看Nginx状态
```bash
sudo systemctl status nginx
sudo nginx -t  # 测试配置
sudo nginx -s reload  # 重新加载配置
```

#### 查看应用日志
```bash
sudo tail -f /var/log/nginx/access.log
sudo tail -f /var/log/nginx/error.log
```

#### 重启Nginx
```bash
sudo systemctl restart nginx
```

---

## 🔄 更新部署流程

当代码有更新时，按以下步骤重新部署：

### 1. 本地提交代码到GitHub
```bash
cd /Users/shanshan/Documents/sss/jupas-frontend
git add .
git commit -m "更新说明"
git push origin main
```

### 2. 服务器拉取最新代码
```bash
ssh admin@112.124.68.82 "cd ~/jupas-frontend && git pull"
```

### 3. 重新构建
```bash
ssh admin@112.124.68.82 "cd ~/jupas-frontend && source ~/.nvm/nvm.sh && npm install && npm run build"
```

### 4. 刷新浏览器缓存
由于是静态文件，无需重启服务，直接刷新浏览器即可看到更新
```bash
# 如果需要，可以重新加载nginx
ssh admin@112.124.68.82 "sudo nginx -s reload"
```

### 一键部署脚本
```bash
# 在本地执行
cd /Users/shanshan/Documents/sss/jupas-frontend
git add . && git commit -m "更新" && git push origin main
ssh admin@112.124.68.82 "cd ~/jupas-frontend && git pull && source ~/.nvm/nvm.sh && npm install && npm run build"
```

---

## 🌐 访问方式

### 开发环境（本地）
- 地址：http://localhost:3001/
- 说明：本地开发调试使用

### 生产环境（阿里云）
- 地址：http://112.124.68.82/jupas/
- 说明：线上正式环境，通过nginx反向代理，与xunji项目复用80端口

---

## 📋 测试账号

### 管理员账号
- 用户名：`admin`
- 密码：`admin123`

### 学生账号
- 用户名：`student1`
- 密码：`123456`
- 权益点数：10

---

## 🛠 技术栈

- **前端框架：** React 18
- **路由：** React Router 6
- **构建工具：** Vite（base: /jupas/）
- **Web服务器：** Nginx
- **版本控制：** Git + GitHub
- **部署方式：** SSH + Nginx静态托管

---

## 📝 项目结构

```
jupas-frontend/
├── dist/                 # 构建输出目录（生产环境）
├── src/                  # 源代码
│   ├── components/       # 公共组件
│   ├── pages/           # 页面组件
│   ├── utils/           # 工具函数和Mock数据
│   ├── constants/       # 常量定义
│   └── styles/          # 全局样式
├── package.json         # 项目配置
├── vite.config.js       # Vite配置
├── README.md            # 项目说明
├── 启动说明.md          # 本地启动说明
└── 部署说明.md          # 本文件

```

---

## ⚠️ 注意事项

1. **端口复用：** 应用通过/jupas/路径访问，与xunji项目复用80端口
2. **目录权限：** /home/admin目录已设置为755权限，允许nginx访问
3. **Node版本：** 服务器需要Node.js 16+版本用于构建
4. **Nginx配置：** 配置文件位于/etc/nginx/nginx.conf
5. **Base路径：** Vite构建时base设置为/jupas/，匹配nginx路由
6. **数据持久化：** 当前使用LocalStorage，生产环境建议接入真实后端API
7. **浏览器缓存：** 更新后如页面未变化，请清除浏览器缓存（Ctrl+Shift+R）

---

## 🔐 安全建议

1. 修改默认测试账号密码
2. 配置HTTPS证书（使用Let's Encrypt）
3. 设置Nginx反向代理（推荐）
4. 定期备份代码和数据
5. 配置日志监控和告警

---

## 📞 问题排查

### 应用无法访问
```bash
# 检查PM2状态
pm2 status

# 查看应用日志
pm2 logs jupas-frontend

# 检查端口占用
netstat -tunlp | grep 3002
```

### 更新后页面未变化
```bash
# 清除浏览器缓存
# 或在浏览器中强制刷新（Ctrl+Shift+R / Cmd+Shift+R）

# 确认dist目录已更新
ssh admin@112.124.68.82 "ls -lh ~/jupas-frontend/dist"
```

---

## 🎉 部署完成时间

- **GitHub推送：** 2025-12-14
- **阿里云部署：** 2025-12-14
- **PM2进程ID：** 1

---

**祝使用愉快！** 🚀

